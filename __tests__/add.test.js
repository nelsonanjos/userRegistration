// import React from 'react';

// import { render, waitForElement } from '@testing-library/react';

// import AddUser from '../src/pages/users/add/index';

const i = require('./test');

describe('MyComponent', () => {
    it('works', () => {
        expect(ola()).toBe('Ola')         // const { getByTestId } = render(<AddUser/>);
        // const fildeNode = await waitForElement(
        //     () => getByTestId('user-add-form')
        // )
        //  console.log(fildNode);   
    })
})